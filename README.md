# README

2D text editor with structured change tracking and versioning.

Dependencies: npm, Electron

To build:

```
npm install
```

To run:

```
npm run start
```

You may need to configure Electron.

